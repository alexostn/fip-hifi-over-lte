# Tested environments

Results reported by people other than the author. Entries come from Issues
labelled `test-report`.

Nothing here identifies a private network or a person who did not ask to be
credited. Wi-Fi names, IP and MAC addresses are never recorded.

| Version | System | Network | Audio output | Result | Report |
|---|---|---|---|---|---|
| v16.3 | Ubuntu, managed lab machine (no sudo) | Wi-Fi | built-in | works via `ao=pulse` fallback | [#1](../../issues/1) |
| | | | | | |

Legend for **Result**:

- `works` — followed the document, got the documented outcome
- `works, docs unclear` — worked, but a step needed guessing
- `partial` — played, but with dropouts, or Bluetooth without LDAC
- `fails` — did not reach playback

## Testers

Thanks to the people who ran these scenarios on machines that are not mine:

- _(first external report goes here)_

If you sent a report and want your name, handle or nothing at all listed here,
say so in the Issue.
